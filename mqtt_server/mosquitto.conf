# enable persistence so clients can reconnect after restarts
persistence true
persistence_location /mosquitto/data/

# listen on default MQTT port
listener 1883

# for simplicity in this lab, allow anonymous clients
allow_anonymous true

# adjust as needed for security in production